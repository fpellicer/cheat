# Resultado de la suma limpio
md5sum -c MD5SUMS 2> /dev/zero | grep coincide   # FALLO o 'FAILED'

# Verificar integridad live usb de Ubuntu
# Ok si no muestra salida
md5sum -c md5sum.txt 2> /dev/null | grep 'no coincide'
