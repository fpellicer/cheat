---
tags: [ packaging ]
---
# Listar todos los snaps instalados en nuestro PC
sudo snap list

# Buscar un snap
sudo snap find <palabra_a_buscar>

# Instalar un snap
sudo snap install <paquete>

# Instalar una versión concreta de un snap (ej. Channel: esr/beta)
sudo snap install firefox --channel esr --beta

# Actualizar un snap
sudo snap refresh <paquete>

# Actualizar todos los snaps
sudo snap refresh

# Volver a la versión anterior del snap
sudo snap revert <paquete>

# Borrar snap
sudo snap remove <paquete>

# Actualizar snap-store
snap-store --quit
sudo snap refresh snap-store
